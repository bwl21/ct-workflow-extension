# Releases Directory

Dieses Verzeichnis enthält die gepackten ZIP-Archive für ChurchTools Extension Deployments.

ZIP-Dateien werden automatisch mit folgendem Format erstellt:
`[kürzel]-[tag]-[commit].zip`

Beispiel: `meinKi-v1.0.0-a1b2c3d.zip`

## Verwendung:

```bash
# Build und Package erstellen
npm run deploy

# Nur Package erstellen (nach manuellem Build)
npm run package
```